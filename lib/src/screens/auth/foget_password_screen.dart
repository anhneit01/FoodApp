import 'package:flutter/material.dart';

class FogetPasswordScreen extends StatelessWidget {
  static const routeName = '/FogetPasswordScreen';
  const FogetPasswordScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hello'),
    );
  }
}